language: php
dist: xenial

php:
  - 7.2
  - 7.3

before_install:
  - sudo apt-get update
  - sudo apt-get install sqlite

install:
  - composer install --no-interaction

before_script:
  - cp .env.sample .env
  - cp phpunit.sample.xml phpunit.xml

script:
  - ./vendor/bin/phpunit
